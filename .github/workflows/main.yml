name: Build & Deploy Laravel API

on:
  push:
    branches:
      - deploy

env:
  CI: false

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install Composer dependencies
        run: composer install --no-scripts --no-interaction --prefer-dist

      - name: Check if build artifacts exist
        run: |
          if [ ! -d "./public" ]; then
            echo "Build directory 'public' not found."
            exit 1
          fi

      - name: List build directory contents
        run: ls -al ./public  # Adjust path as per Laravel's public directory

      - name: Upload production-ready build files
        uses: actions/upload-artifact@v2
        with:
          name: laravel-production-files
          path: ./public/  # Adjust path as per Laravel's public directory

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/deploy'

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: laravel-production-files
          path: ./public/  # Adjust path as per Laravel's public directory

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@4.2.0
        with:
          server: ftp.opportunitiessharing.com  # Replace with your FTP server address
          username: back@back.opportunitiessharing.com
          password: Naruto2002#10
          local-dir: ./public/  # Ensure this ends with a slash
          server-dir: /public_html/  # Adjust destination directory on FTP server
